# dependencies
node_modules

# build output
dist
.output
.vite
coverage
*.tsbuildinfo

# Expo / React Native (native folders are generated by `expo prebuild`)
.expo
apps/mobile/android
apps/mobile/ios
*.apk
*.aab
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# env
.env
.env.*
!.env.example

# misc
*.log
.DS_Store

# Dados pessoais de localização puxados do telemóvel para depuração (teste de
# campo, sessão 04) — nunca entram no repositório, que é público. Ficam fora
# do repo (ex.: ~/bricklap-campo/); estas regras são só um cinto de segurança
# contra um `git add .` distraído.
*.db
*.db-wal
*.db-shm
*.geojson
gps-raw*
