# Sessão 27 — A ronda 1 abre com o Iniciar, o declarado mostra-se declarado, e a taxonomia de exercícios

**Data**: 2026-09-19 · **Branch**: `feat/taxonomia`, a partir de `main` (b62378f) · **Telemóvel**: por USB · **Estado no fecho**: tudo feito e provado — motor, base, app, documentos, **release instalado e verificado no telemóvel, base provada com uma sessão de teste indoor** — e fundido em `main`. O relatório tem duas partes: as secções 1 a 10 escrevem-se ao longo da sessão (a primeira metade, antes da janela no telemóvel); a **secção 11** é a janela, a prova e a auditoria do treino de hoje, e **a secção 12** são as decisões do fundador que chegaram com ela.

## Estado lido

Li `AGENTS.md`, `STATUS.md`, `ROADMAP.md`, `docs/VISAO.md`, o relatório da sessão 26 e `git log --oneline -30`. O projeto está na Fase 4; a sessão 26 aceitou e implementou o ADR 0011 (rondas e valores, esquema v4) e provou-o pela base num teste indoor do fundador, que deixou três arestas: o bloco de 6 s antes da primeira ronda, o ritmo do remo saído de metros declarados sem o dizer, e o exercício como nome escrito. A 4.5 (planos) é a sessão seguinte e depende da taxonomia. A marca e o nome estão em standby e não se tocaram. Dos níveis B e C li `docs/AMBIENTE.md` (as regras do telemóvel da sessão 26), `docs/DESIGN.md` §6 e §8 (a regra nova vive lá), `LEGAL.md` (texto visível ao atleta e dados de treino — nada aqui cria dados novos nem alegações), `docs/adr/0011`, `docs/adr/0006` e `0008`, os três registos de `docs/dogfooding/`, `ARCHITECTURE.md` e `apps/mobile/README.md`.

## 1. Como resolvi a ronda 1 sem casos-limite

**A decisão do CTO**: o Iniciar abre logo a ronda 1; não é a ronda a absorver blocos curtos.

**Como ficou.** `createLiveSession` escreve dois eventos no mesmo instante, `started` e `round_started`. A derivação já tinha a regra de que precisava: um marcador de ronda que cai em cima do bloco acabado de abrir **marca-o em vez de o partir**. O primeiro bloco nasce na ronda 1, sem nenhum bloco de ~0 s, e o Nova ronda só abre da 2 em diante. Na base, os dois eventos vão **numa transação** (`writeEvents`): uma sessão nunca está no disco com um e sem o outro.

**O Nova ronda logo a seguir ao Iniciar — nem ronda vazia, nem a 1 duplicada.** Três guardas, nenhuma com um limiar de duração na leitura:

1. **Na escrita**, `applyRoundStart` recusa um marcador a menos de `ROUND_DEDUPE_MS` (2000 ms) do início da ronda aberta — a regra que já existia para o duplo disparo do botão, e que agora conta a partir do Iniciar, porque é lá que a ronda 1 abre. O toque não se escreve, e **um toque que não se escreve não perde nada**: o tempo continua onde estava, na ronda 1. É a diferença para a saída rejeitada, em que um bloco *já gravado* desaparecia.
2. **Na derivação**, a regra é de **ordem**, não de duração: um marcador que não vem *depois* da ronda já aberta é essa ronda dita duas vezes e não abre nada (`roundStartsFromEvents`, e a mesma regra em `blocksFromEvents`, para rondas e blocos nunca discordarem). Um registo com dois `round_started` no mesmo instante — escrito por outra versão, ou à mão — lê-se com uma ronda e um bloco.
3. **`applyRoundStart` recusa um instante anterior ao bloco aberto**: uma ronda não começa dentro de um bloco já fechado. Com relógio monótono isto nunca acontece; a guarda fecha a porta a uma ronda que ficasse sem blocos.

**A prova de que não há casos-limite** é um teste de propriedade (`packages/engine/test/rounds.test.ts`): 300 sequências geradas de Mudar, Marca e Nova ronda, com intervalos de 0 ms, 1 ms, 500 ms, 1999 ms, 2000 ms, 6 s e 90 s, com e sem Parar. Em todas: a ronda 1 começa no instante da sessão; os inícios das rondas são estritamente crescentes (nunca duas no mesmo instante); **todo o bloco está numa ronda e toda a ronda tem pelo menos um bloco**; e nenhum marcador de ronda fecha sozinho um bloco de 0 ms.

**O que isto não faz, dito às claras.** Se o atleta carregar em Nova ronda aos 6 s, como o fundador fez no teste, isso é agora **a ronda 2, pela mão dele**, com "Ronda 1" no ecrã antes do toque: o registo diz o que aconteceu e os 6 s ficam onde foram vividos, na ronda 1. O que mudou é que **deixou de ser preciso carregar** — foi a obrigação de abrir a primeira ronda à mão que criou o bloco de 6 s. Para o toque não ser equívoco, a pílula "Ronda 1" está no ecrã desde o primeiro segundo e o botão passou a dizer a ronda que abre: **"Nova ronda · 2"**.

**As sessões antigas.** Não têm nenhum `round_started`, e nada lho acrescenta: leem-se sem rondas, com `round: null` em todos os blocos, como sempre. **Provado sobre a base real** (secção 5). No resumo, os cabeçalhos de ronda só se desenham com duas ou mais rondas (ou, numa sessão antiga, com blocos antes da primeira): uma corrida de um bloco não ganhou um "Ronda 1" por cima.

## 2. O que vem de declarado mostra-se como declarado

**A regra**, escrita no [ADR 0011 §3a](../adr/0011-rondas-e-valores-registados.md) e no [DESIGN.md §6](../DESIGN.md) ("Valor declarado"): qualquer valor derivado de um declarado mostra-se como declarado — o ritmo, a distância, o *split*, a velocidade, e o que vier a existir.

**No motor.** `derivedOrigin(...entradas)` é a regra numa função: basta uma entrada declarada para o resultado ser declarado; todas as derivações passam por ela. `aggregateBySport` deixou de ter *um* ritmo por desporto: devolve **dois totais por origem**, `measured` e `declared`, cada um com os seus metros, o seu tempo e o seu ritmo. **Não existe no tipo nenhum campo onde os dois se somem** — o defeito da sessão 26 era exatamente esse campo (`secPerKm` calculado sobre `measuredM + declaredM`). Um desporto com as duas origens (um bloco de remo importado de um dispositivo ao lado de um escrito à mão, um dia) tem dois ritmos e nenhum misturado.

**Na app.** A marca é **a palavra `decl.`**, colada ao número (`SeloDeclarado`): na linha de cada bloco com valores declarados, no valor calculado da ficha, na linha declarada do "Por desporto"; e o resumo de uma sessão com valores declarados fecha com uma legenda ("decl. = declarado por ti, não medido. O que se calcula a partir de um valor declarado também é declarado."). Um bloco com GPS nunca a leva. Escolhi a palavra, e não uma cor, um peso de letra, um til ou um ícone, porque tem de se ler ao sol, nos dois temas e a uma cor; porque um til diz "aproximado" e um valor declarado não é aproximado, é não verificado; porque um ícone novo tinha de se aprender; e porque um leitor de ecrã lê palavras. **É uma proposta**: a marca vive num componente só, e trocá-la é uma linha.

### O caso misto — GPS e declarado na mesma sessão (o treino 02)

**O que implementei (proposta A): dois números, nunca um.** No cabeçalho do resumo, a distância medida fica onde estava; quando a sessão também tem metros declarados, a linha medida passa a dizer "3,28 km **medidos**" e por baixo aparece "1,00 km **declarados**". No "Por desporto", cada desporto tem uma linha base (blocos e tempo) e **uma linha por origem**: "3,28 km · 8:43/km" para a corrida; "1 000 m · 2:00/500 `DECL.`" para o remo.

**As alternativas que considerei, e porque não:**

| | Como se via | A favor | Contra |
|---|---|---|---|
| **A — duas linhas** (implementada) | "3,28 km medidos" / "1,00 km declarados" | nunca existe um número que seja metade facto, metade afirmação; o total medido continua a ser o que alimenta recordes | ocupa duas linhas; quem vem do Strava procura um total |
| **B — total com discriminação** | "4,28 km · 3,28 medidos + 1,00 declarados" | um número de relance, o que as outras apps dão | o número grande é a soma de um facto com uma afirmação — é o número que alguém um dia compara com outra semana; e pela regra 3a essa soma **é declarada**, o que tira à parte medida o estatuto que tem |
| **C — só o medido nos totais** | "3,28 km"; o declarado vive nos blocos e no "Por desporto" | totais 100 % medidos, o mais simples | uma sessão só de ginásio fica sem distância nenhuma no cabeçalho, com 2 km de remo feitos; esconde trabalho precisamente no treino que o dogfooding mede |
| **D — soma marcada como declarada** | "4,28 km `DECL.`" | é a regra 3a à letra | uma corrida de 10 km com 200 m de passadeira passava a "10,2 km declarados": degrada o que foi medido |

**Recomendo a A**, pela razão de fundo do brief: a distinção está na base desde o primeiro dia para o primeiro recorde pessoal não ser falso, e a única maneira de um total nunca o falsificar é não haver total. Vale também para o que ainda não existe — totais semanais, histórico, recordes: dois contadores, nunca um. **A escolha é do fundador e do CTO**; a B é a única outra que eu defenderia, se o fundador sentir falta do número único, e nesse caso com o total sempre acompanhado das duas parcelas.

## 3. A taxonomia de exercícios

Está no [ADR 0012](../adr/0012-taxonomia-de-exercicios.md) — **aceite** (ver secção 12). O essencial:

- **Dois eixos, e só um é novo.** O **desporto** do segmento já existia e decide *como se grava* (GPS ou não, que campos). O **tipo de exercício** é novo, só existe dentro de um bloco de Força, e existe por uma razão: **decide que campos o bloco mostra**.
- **Um tipo é código; um exercício é um dado.** Os tipos são dois — os que os treinos reais pedem.

**Os exercícios reais do fundador, mapeados** (treino 01: 500 m remo, 10 flexões, 10 bicep com haltere, 500 m passadeira, 5 push ups, 10 RDL, quatro rondas):

| Exercício real | Onde cai | Campos | Identidade |
|---|---|---|---|
| **Remo indoor** | desporto `rowing_indoor` (cardio em máquina) | metros, *split* da máquina opcional | `sport:rowing_indoor` |
| **Passadeira** | desporto `treadmill` (cardio em máquina) | km/h **ou** metros, o outro derivado | `sport:treadmill` |
| **Flexões** e **push ups** | **um só** exercício, `flexoes`, tipo **peso do corpo** — a mesma coisa em duas línguas (decisão do fundador, secção 12); os 10 e os 5 da ronda 1 somam 15 | repetições | `ex:flexoes` |
| **Bicep com haltere** | exercício `bicep_haltere`, tipo **peso livre** | repetições, carga | `ex:bicep_haltere` |
| **RDL** | exercício `rdl`, tipo **peso livre** (com carga, confirmado) | repetições, carga | `ex:rdl` |

- **O que ficou de fora**: *máquina de musculação* (mesmos campos do peso livre e nenhum exercício real) e *isométrico* (idem). Cada um é uma linha de código no dia em que um treino o pedir. Não há lista genérica de ginásio.
- **Os nomes comparam-se dobrados**: sem maiúsculas, **sem acentos** ("flexoes" é o que o teclado escreve a meio de um treino), hífenes e pontos como espaços. "bicep" — o único nome alguma vez escrito na app do fundador — é uma grafia de `bicep_haltere`.
- **Um exercício novo entra sem programador**: o fundador escreve o nome na ficha e escolhe o tipo em duas pílulas ("Peso livre · repetições e carga", "Peso do corpo · só repetições"). O evento leva o nome e o tipo; a partir daí o exercício é uma entrada do catálogo, **derivada do registo** (`catalogFromMentions`), e a sessão seguinte oferece-o com o tipo já posto. **Não há tabela de exercícios**: apagar as sessões que o usaram tira-o do catálogo. A última palavra sobre um tipo é do atleta, mesmo para um exercício de partida.
- **A ligação à identidade do ADR 0011 §1b**: `exerciseIdentity` dá o id do catálogo a um nome conhecido e o nome dobrado a um desconhecido; `blockIdentity` dá a um bloco a identidade do exercício nomeado ou, sem nome, **o desporto quando o desporto é um exercício em si** (o remo compara-se com o remo sem ninguém o nomear). O teste usa o treino 01 com a ronda 2 a saltar o bicep: o RDL é o 6.º bloco da ronda 1 e o 5.º da 2 — pela posição tinha sido emparelhado com os push ups; pela identidade alinha.
- **O que se guarda é o que o atleta escreveu, nunca um id.** A resolução faz-se ao ler: juntar duas entradas é acrescentar uma grafia, e o histórico melhora sozinho quando o catálogo melhora.

**Na ficha**: o nome, as sugestões do catálogo (as mais recentes primeiro), as duas pílulas de tipo — a ativa segue o nome escrito quando o catálogo o conhece — e **só os campos desse tipo**. As flexões deixaram de pedir carga. Um bloco que passa de bicep a flexões limpa a carga com um `recorded` de `loadKg: null`; os 12,5 kg continuam no registo.

## 4. O que fiz, por pacote

- **Motor** (`packages/engine`): `createLiveSession` com a ronda 1; `roundStartsFromEvents`; a regra de ordem em `blocksFromEvents`; `applyRoundStart` com a guarda do bloco aberto; `exercises.ts` novo (catálogo de partida, `foldExerciseName`, `resolveExercise`, `exerciseIdentity`, `catalogFromMentions`); em `records.ts`, `kind` no `recorded`, `fieldsOfKind`, `blockKind`, `fieldsOfBlock`, `blockIdentity`, `identityAcrossRounds`, `exerciseMentions`, `derivedOrigin`, e `aggregateBySport` por origem. **293 testes em 16 ficheiros, cobertura 100 %** nas quatro métricas.
- **Base** (`apps/mobile/persistence`): `start` escreve os dois eventos numa transação; o `kind` viaja no JSON do `payload` (um `kind` desconhecido falha alto, com o `seq`); `exerciseCatalog()` deriva o catálogo das linhas `recorded`. **Sem esquema novo: continua v4.**
- **App**: a pílula e o botão da ronda; os cabeçalhos de ronda só quando organizam alguma coisa; `SeloDeclarado` nas linhas, na ficha e no "Por desporto"; a legenda; as duas linhas dos totais; a ficha com sugestões do catálogo, tipo e campos por tipo. Dez chaves novas nos dois dicionários.
- **Documentos**: ADR 0011 (§1a e §3a), ADR 0012 (novo), DESIGN.md §6 (três componentes), ARCHITECTURE.md, ROADMAP.md (4.5a′), BACKLOG.md, `apps/mobile/README.md`, `docs/AMBIENTE.md` fica como estava.

## 5. Migração, provada contra a base real

**Não há migração de esquema nesta sessão**, e é de propósito: o tipo cabe no `payload` do v4, e os nomes resolvem-se ao ler. "Migração" aqui quer dizer duas coisas, e provei as duas sobre a base real do telemóvel (a cópia puxada no fecho da sessão 26, depois de apagada a sessão de teste — **57 sessões, 243 eventos, 10 051 amostras**, esquema 4):

1. **As sessões antigas abrem e leem-se bem, e nada se perde.** `apps/mobile/test/leitura-real.test.ts` abre todas as sessões pelo caminho da app e escreve um resumo de **tudo o que a app deriva de cada uma**: tipos de evento, número de amostras, **os 103 blocos** com as suas fronteiras e a sua ronda, as rondas, os segmentos, a duração, a distância e os valores registados. O ficheiro só usa o que o motor já exportava antes desta sessão, de propósito: **corri-o também num checkout de `main` (b62378f), com o `npm ci` dele, sobre a mesma base, e comparei os dois resumos: 51 900 bytes cada, md5 `c465a095b91232445a867af8fe9c8863` nos dois — iguais byte a byte.** "As sessões antigas leem-se como antes" é um `cmp` sem saída, não uma frase. Em todas: zero rondas, `round: null` em todos os blocos.
2. **Os nomes escritos sobrevivem e mapeiam onde dá.** A base do fundador tem hoje **zero nomes escritos** — o único, "bicep", estava na sessão de teste de 2026-09-18 que ele mandou apagar. `taxonomia-real.test.ts` repõe as oito linhas dessa sessão **só na cópia temporária**, tal como o relatório da sessão 26 as registou, e prova: a sessão lê-se pela regra com que foi gravada (o bloco de 6,4 s sem ronda, depois a ronda 1 — nada se reinterpreta para trás); o texto guardado continua "bicep"; resolve para `bicep_haltere`, peso livre, com os 22,5 kg; o remo tem identidade `sport:rowing_indoor` e os 450 m da correção. Depois grava uma sessão nova na mesma base — ronda 1 na primeira linha, um Nova ronda aos 500 ms que não escreve nada, umas flexões cuja carga é recusada, um "Kettlebell swing" que entra no catálogo pelo tipo escolhido — e confirma que **as 243 linhas antigas estão iguais campo a campo**, as 57 sessões se repetem iguais, e `integrity_check = ok`.

A prova v3 → v4 da sessão 26 continua no repositório e passa a saltar-se sozinha numa base já migrada.

## 6. O telemóvel — o que não fiz na primeira metade, e porquê

Os dois APK estavam construídos e **não instalei nenhum**. Antes de instalar verifiquei, sem tocar no ecrã, se havia gravação em curso — um `pm install` mata o processo —, e li o `LocationTaskService` a correr há 18 minutos como um treino do fundador. **Li mal**: era um registo de serviço já fora do primeiro plano, de uma sessão parada às 12:02 (secção 11.2). O custo foi mais de uma hora de espera e uma primeira versão deste relatório a dizer que o telemóvel estava a gravar. A regra que ficou escrita em [AMBIENTE.md](../AMBIENTE.md) e na memória do agente é a certa: **a prova de uma gravação viva é `isForeground=true`, não a existência do serviço.**

## 7. Validação

- `npm test` (depois da secção 12): **386 passaram, 3 skipped** (as três provas sobre a base real, que correm à parte com `BRICKLAP_DB` e **passaram sobre a base de hoje**, com as quatro sessões novas lá dentro).
- `npm run test:coverage`: **motor a 100 %** em declarações, ramos, funções e linhas; o adaptador de persistência a 99 % (as linhas por cobrir são as guardas defensivas que já lá estavam).
- `npm run typecheck`, `npm run build:web`, `npm run export:android -w @bricklap/mobile`: verdes. O DoD completo corre em `main` depois do `npm ci` (secção 11.6).
- Release e variante depurável reconstruídos com as decisões do fundador: `BUILD SUCCESSFUL`, os dois.
- **Release instalado e verificado**: `base.apk` no telemóvel com md5 `b4e9b9e3682c227585ecf70f68940230` = o do PC; não depurável (`run-as` recusa).

## 8. O que decidi sem o brief mandar

- **Escrever o `round_started` no Iniciar, em vez de derivar a ronda 1 do `started`.** Derivar mudava a leitura de todas as sessões antigas (ganhavam uma ronda que ninguém marcou) e a das sessões da sessão 26 (o bloco antes da primeira ronda passava a "Ronda 1" e o resto deslocava-se). Com o evento escrito, cada sessão lê-se pela regra com que foi gravada.
- **O botão diz a ronda que abre** ("Nova ronda · 2"), e os cabeçalhos de ronda só aparecem com duas ou mais.
- **A marca de declarado é a palavra `decl.`**, e a apresentação do caso misto é a A — as duas como propostas (secção 2).
- **`aggregateBySport` mudou de forma** (dois totais por origem em vez de campos soltos): é uma quebra de API dentro do repositório, sem consumidores fora dele.
- **O tipo de um exercício escolhido à mão pertence ao exercício**: mudar o nome do bloco larga-o; o mesmo exercício noutra grafia mantém-no.
- **RDL como peso livre, flexões e push ups como duas entradas** — as duas eram propostas; o fundador decidiu (secção 12).
- **Não mudei o rótulo "Força"** — o fundador mudou-o para "Ginásio" (secção 12).
- **Não instalei** com o que li como uma gravação em curso — e li mal (secção 6).

## 9. Dúvidas para o CTO

1. **`ROUND_DEDUPE_MS` continua a ser um número** (2000 ms). Mantive-o porque guarda a *escrita* — um toque que não se escreve não perde nada — e não a leitura, que era a objeção ao limiar de duração. Se a objeção também o apanha, a alternativa é recusar só o mesmo instante e aceitar que um duplo disparo a 300 ms faça uma ronda de 300 ms.
2. **O aquecimento passa a ser a "Ronda 1".** No treino 02 (corrida na rua, depois o circuito), a corrida é a ronda 1 e o circuito começa na 2. A comparação entre rondas é por identidade e não se parte; o que desalinha é o número que o atleta lê. Os planos (4.5) sabem o que é aquecimento — resolve-se lá?
3. **Um Nova ronda por engano não se desfaz** — como uma Marca. Com a ronda 1 aberta no Iniciar o engano mais provável desapareceu; se reaparecer num treino real, é um evento `round_cancelled` a acrescentar, não uma reescrita.
4. **O catálogo do atleta desaparece com as sessões** que o criaram. É o comportamento certo para o apagamento; os planos (4.5) passam a ser a segunda fonte de menções.
5. **O lab web também passou a criar sessões na ronda 1**, porque a regra está no motor. O lab não mostra rondas e os testes dele passam; digo-o para não ser surpresa.

## 10. Perguntas para o fundador — as da primeira metade estão respondidas (secção 12)

As perguntas 1 a 4 da primeira versão deste relatório (flexões = push ups?, RDL com carga?, "Força"?, caso misto A ou B e a marca `decl.`) têm resposta na secção 12. **Ficam abertas:**

1. **O que aconteceu às 12:01 de hoje?** A base tem uma sessão de Transição de 1:24, parada, e nenhum treino (secção 11.1). Foi um teste? Um toque no botão errado? O treino ficou no Garmin?
2. **O cabeçalho "GINÁSIO" da secção de desportos sem GPS e o botão "Ginásio" dizem a mesma palavra** no ecrã inicial e no Mudar (secção 11.4). O botão é o desporto (`strength`); o cabeçalho era o grupo dos desportos sem GPS. Um dos dois muda de nome — qual?
3. As perguntas da secção 10 do relatório da sessão 26 (rondas, exercícios e valores dos treinos 02 e 03) continuam por responder.

## 11. A janela no telemóvel, a prova e a auditoria de hoje

O fundador deu a janela (telemóvel pousado e desbloqueado). Antes de cada toque confirmei que o Bricklap estava à frente (`topResumedActivity`) e que não havia gravação com `isForeground`. Correu nesta ordem.

### 11.1 A auditoria de "o treino de hoje" — não há treino

O registo completo está em [docs/dogfooding/2026-09-19-treino-04.md](../dogfooding/2026-09-19-treino-04.md). **A base do telemóvel (58 sessões, `integrity_check = ok`, esquema v4) tem uma sessão de hoje e não é um treino**: 12:01:03 → 12:02:28, **1:24 (84 393 ms)**, desporto Transição, **dois eventos** (`started`, `stopped`), **11 amostras** de GPS, um telemóvel parado — 38,6583 N, −9,0709 O, velocidade ≤ 0,20 m/s, **2,49 m** de distância medida em 84 s.

- **Rondas: nenhuma** (a app da sessão 26 não abre a ronda 1 no Iniciar; ninguém tocou em Nova ronda). Um bloco; nenhum bloco de ~0 s.
- **Valores registados: zero. A ficha não foi aberta.** Dito sem o suavizar: **não há, na base, um só valor registado durante um treino a sério.** Mas esta sessão não responde à pergunta do dogfooding (a ficha usa-se a meio de um treino?), porque não foi um treino.
- **GPS**: sem buracos (intervalo máximo 10,1 s), precisão a melhorar de 24 m para 5 m. **Amostras a menos face ao pedido**: a app pede uma por segundo e chegou uma a cada ~9 s; parado e talvez em interior é plausível, mas nenhuma sessão parada tinha sido medida.
- A sessão sobreviveu inteira; **a duração (84 393 ms) é igual à soma dos blocos** (um). Custo de escrita: 9 lotes em 37,1 ms, 10,0 ms no máximo.
- **Por confirmar pelo fundador**, marcado no registo: se houve treino hoje e onde ficou; porque foi a sessão em Transição; rondas, valores e exercícios.

### 11.2 O serviço de localização parou ao Parar (a leitura errada que corrigi)

O `logcat` do telemóvel (que cobre o Parar) mostra a notificação do serviço em primeiro plano removida às 12:02:28.111, 26 ms depois do `stopped` das 12:02:28.083, e o último lote de localização às 12:02:26. O registo de serviço que li às 12:19 estava fora do primeiro plano (a app usa `killServiceOnDestroy: false`). **Não há um problema da app**; havia um problema meu de leitura.

### 11.3 A sessão de teste indoor e a prova pela base

Instalei a variante depurável (md5 `780694bf…`), corri as duas provas da secção 5 sobre a base de hoje (passaram), e gravei **duas sessões indoor sem GPS** (o serviço de localização nunca apareceu):

**Sessão A** (`mu8bbzq0-zm577v`, 4:49): Remo indoor → registar 500 m → Mudar para Ginásio → registar "Flexões" (do catálogo) 10 → premir a Marca → registar **"push ups"** 5 (digitado) → Nova ronda · 2 → registar **"Kettlebell swing"**, tipo Peso livre, 12 × 16 kg → Parar. Os eventos, tal como estão na base:

| seq | evento | t | notas |
|---|---|---|---|
| 272 | `started` rowing_indoor | 0,000 s | |
| **273** | **`round_started`** | **0,000 s** | **o mesmo instante do `started`** |
| 274 | `recorded` block 0 | 59,1 s | `{"origin":"declared","values":{"meters":500}}` |
| 275 | `sport_changed` strength | 70,1 s | |
| 276 | `recorded` block 1 | 126,9 s | `"exercise":"Flexões","values":{"reps":10}` — **sem `kind`** |
| 277 | `marked` | 197,5 s | |
| 278 | `recorded` block 2 | 233,3 s | `"exercise":"push ups","values":{"reps":5}` — **sem `kind`**, e o texto é o digitado |
| 279 | `round_started` | 248,5 s | o Nova ronda · 2 |
| 280 | `recorded` block 3 | 286,4 s | `"exercise":"Kettlebell swing","kind":"free_weight","values":{"reps":12,"loadKg":16}` — **o único `kind`** |
| 281 | `stopped` | 289,3 s | |

- **`round_started` no mesmo instante do `started`**: sim (sessões A, B e C).
- **`kind` no exercício novo e ausente nas flexões**: sim — o único `kind` é o do Kettlebell; "Flexões" e "push ups" não o têm (o catálogo sabe-o, a base não guarda o que sabe).
- **Tudo declarado**: os quatro `recorded` têm `origin: "declared"`. No ecrã: `DECL.` em cada linha, o cabeçalho "**500 m declarados**", e a legenda no fim; o *split* de 1:10/500 que a app calculou também leva `DECL.`.
- **0 amostras** nas sessões de teste (a base tem as 11 da sessão do fundador e mais nenhuma).
- **As flexões e os push ups somam 15 na ronda 1**: no resumo aparecem como dois blocos ("Flexões 10×", "push ups 5×") e `exerciseTotalsByRound` dá 15 (teste), sem se reescrever uma linha. **O resumo não mostra ainda esse total** — é um agregado do motor à espera de uma superfície (histórico, 4.5).
- **Resumo**: "4 blocos · 2 rondas", "Ronda 1" (remo, flexões, push ups) e "Ronda 2" (Kettlebell); "Por desporto": Remo indoor 500 m · 01:10/500 DECL.; Ginásio 3 blocos · 03:39.

**Sessões B e C — o Nova ronda logo a seguir ao Iniciar não escreve nada.** B (`mu8bj3wc-ch0o2z`): `started`, `round_started` (0,000 s), `stopped` — só isso. C (`mu8bl9il-4jekur`): igual, mas com uma escada de **quatro toques em Nova ronda** aos ~0,4, 0,7, 1,0 e 1,3 s do Iniciar: **nenhum marcador, nenhuma segunda ronda** — `started > round_started > stopped`. A base prova que **nada foi escrito**; não distingue, dos quatro toques, os que a guarda de 2 000 ms absorveu dos que chegaram antes de o ecrã de gravação estar de pé (a guarda é a do teste de propriedade, 300 sequências, incluindo a 0 ms). O que a prova tem de sólido é o resultado, não a contagem de toques. Na pílula: "Ronda 1" e "Nova ronda · 2" o tempo todo.

**As 57 sessões antigas, iguais:** os **243 eventos com `seq ≤ 261` iguais linha a linha** entre a cópia da sessão 26 e a base de hoje (depois de instalar, gravar e apagar); as 10 051 amostras iguais; o resumo do que a app deriva das 57 sessões antigas — corrido com o código de `main` e com o novo, sobre a base de hoje — **igual sessão a sessão: 57 iguais, 0 diferentes**.

### 11.4 O que o telemóvel mostrou de errado (achados, não decisões)

Os achados abaixo são leituras do ecrã real; nenhum está corrigido, e vão para o [BACKLOG](../BACKLOG.md).

- **"GINÁSIO" duas vezes**: o cabeçalho da secção dos desportos sem GPS e, logo por baixo, o botão do desporto "Ginásio" (no ecrã inicial e no Mudar). Consequência do renome; pergunta 2 da secção 10.
- **A ficha de valores** (sessão 26, não da 27): **"declarado" parte-se em "declarad/o"** no rótulo do *Split*; **"Repetições" parte-se em "Repetiç/ões"**; o **Guardar e o Depois ficam em parte por baixo da barra de navegação** (só a metade de cima do botão é tocável); e o **teclado numérico tapa a ficha inteira** — o campo que se está a escrever e o Guardar deixam de se ver até se carregar em "Fim". É a ficha que o fundador vai ter de usar com as mãos suadas, e já falha à vista com as minhas.
- A **Marca é de premir e manter** (um toque de 100 ms não a dispara; assim foi desenhada), o que apanhou o meu `adb` e não deve apanhar o fundador.
- A lista de blocos da gravação mostra só os blocos fechados; o bloco aberto está no cartão de cima, e o último bloco só entra na lista quando fecha.

### 11.5 O que deixei no telemóvel

- **Release da sessão 27, `b4e9b9e3682c227585ecf70f68940230`**, igual ao do PC (verificado no `base.apk` instalado), não depurável.
- **Base: 58 sessões** — as 57 antigas e a de hoje do fundador (12:01), que **não apaguei**. As três sessões de teste (12:37, 12:42, 12:44) foram apagadas pelo histórico da app; `integrity_check = ok`, nenhuma amostra órfã.
- Ficheiros temporários no `/data/local/tmp` e no disco do Windows removidos.

### 11.6 Fusão em `main`

`feat/taxonomia` fundida em `main` com `--no-ff` (`136909c`). **DoD completo em `main`, depois do `npm ci`**: `npm test` **386 passaram, 3 skipped**; cobertura do motor **100 %** (nenhum ficheiro do motor aparece na lista dos abaixo de 100; o total do repositório é 99,65 % de declarações, pelas guardas do adaptador de persistência); `npm run typecheck` limpo; `npm run build:web` verde; `npm run export:android -w @bricklap/mobile` verde. `main` empurrado e igual ao remoto.

## 12. As decisões do fundador que chegaram com a janela

- **Flexões e push ups são o mesmo exercício.** Uma só entrada, a outra é uma grafia. **O padrão, não só o caso**: o fundador escreve em português ou em inglês conforme lhe vem à cabeça, por isso o catálogo de partida traz os pares óbvios já ligados (flexões/push ups, barras/pull ups, agachamento/squat, levantamento-terra/deadlift, RDL/romanian deadlift/peso morto romeno, e mais seis: abdominais/sit ups, bicep/dumbbell curl, avanços/lunges, supino/bench press, press de ombros/shoulder press, remada com haltere/dumbbell row) e **uma entrada tem uma lista de grafias em pt e en** — escrito no ADR 0012 §2. Onze entradas.
- **O que a junção faz aos dados**: no treino 01 os "10 flexões" e os "5 push ups" da mesma ronda são dois blocos do mesmo exercício e **somam 15** (`exerciseTotalsByRound`, novo); a identidade é uma só; **o texto guardado na base não se reescreve** (o teste confirma que os dois blocos continuam a dizer "Flexões" e "push ups"). Repetições somam, a carga é a mais pesada e nunca se soma, e a origem herda-se.
- **O RDL é com carga**, peso livre — confirmado. O "por confirmar" saiu do ADR.
- **O desporto "Força" passa a "Ginásio"**, só o rótulo, nos dois dicionários (`Ginásio` / `Gym`); o identificador `strength` na base não mudou e nenhuma sessão antiga se reescreve. Aplicado na app (aparece no ecrã inicial, no Mudar, no resumo e nas listas).
- **Caso misto: proposta A, duas linhas, sem total único**, e o mesmo para os totais semanais e o histórico quando existirem: **dois contadores, nunca um**. Escrito no ADR 0011 §3a (extensão) e no DESIGN.md §6 — a razão é que a única forma de um total nunca falsificar um recorde é não haver total.
- **A marca `decl.` fica como está**: uma palavra, e não uma cor ou um ícone, porque se lê ao sol, nos dois temas e num leitor de ecrã.
- **ADR 0012: aceite.**

## Próximos passos

1. **4.5, planos de treino** — com exercícios por identidade (ADR 0012) e o previsto comparado com o feito pela mesma regra que compara rondas.
2. **A ficha para as mãos suadas**: o teclado que a tapa, o Guardar por baixo da barra de navegação, os rótulos que se partem (secção 11.4). O treino que a vai pôr à prova ainda não aconteceu.
3. **Um treino a sério com a app** — o primeiro que se pode auditar com a ronda 1 aberta pelo Iniciar e o catálogo de exercícios.
4. A ficha a partir do histórico, depois da 4.5.
