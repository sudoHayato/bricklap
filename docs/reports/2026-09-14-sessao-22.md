# Sessão 22 — Tornar o repositório independente da ferramenta (só documentação)

**Data**: 2026-09-14 · **Branch**: `feat/agents-md`, a partir de `main` (`c7113aa`) · **Não fundida em `main`**

O primeiro brief desta sessão pedia quatro linhas anexadas ao relatório da sessão 20; o segundo, que alargou o âmbito, pede um relatório com a data de hoje. Segui o segundo.

## 1. Estado lido

Li `STATUS.md`, `ROADMAP.md` e `docs/VISAO.md` em `main` (`c7113aa`), e o relatório da sessão 20 (com a 20b e a 21 anexadas).

1. **Fases 1 a 3 concluídas; Fase 4 em curso.** A 4.5 (planos de treino) passou a ser precedida pelo modelo de rondas e valores do [ADR 0011](../adr/0011-rondas-e-valores-registados.md), **proposto**, para o fundador decidir na quarta (2026-09-17).
2. **O primeiro dogfooding está registado** ([treino 01](../dogfooding/2026-09-14-treino-01.md)): a app gravou o tempo do treino, não o treino.
3. **A marca está em standby** por decisão do fundador: P2122 candidata, ligadura BL por testar.
4. **`feat/dogfooding-01` e `feat/marca-blender` fundidos em `main`** na sessão 21, com o DoD verde.
5. **Duas coisas contradiziam o brief, e parei nas duas**: o `.gitignore` ignorava o `AGENTS.md` de propósito, e as diretrizes do CTO não vieram no primeiro brief. O CTO resolveu as duas (§3).

## 2. Feito

| Brief | Estado |
|---|---|
| `AGENTS.md` canónico, `CLAUDE.md` ponteiro | Feito (`8d9ce73`) — o texto passou tal como estava; só "Tu (Claude Code)" passou a "Tu (o agente de código)" |
| 1. `docs/CTO.md` | Feito — transcrição sem alterações, mais a secção nova "Se mudares de agente" |
| 2. `LEGAL.md` na raiz | Feito — requisitos e restrições, com os pontos a validar por advogado marcados, e a secção "o que ainda não está tratado" |
| 3. `docs/NEGOCIO.md` | Feito |
| 4. `docs/AMBIENTE.md` | Feito |
| 5. Contexto de treino | Feito — `docs/dogfooding/README.md`, para o registo do treino 01 continuar a ser só o daquele dia |
| 6. `README.md` | Feito — os seis ficheiros no topo, em três linhas, e o mapa do repositório atualizado |
| `STATUS.md` | Uma linha: a lista de documentação passou a incluir os ficheiros novos e o ADR 0011 |

`git diff --stat main -- apps/ packages/ tools/`: **vazio**. Sem código, sem builds, sem Blender. O DoD dos quatro passos (testes, typecheck, `build:web`, `export:android`) não se aplica, porque não houve código.

## 3. Decisões

1. **Reversão no `.gitignore`.** A linha `AGENTS.md`, com o comentário "Not ours: instruction files for other coding agents", saiu por decisão do CTO nesta sessão: o `AGENTS.md` passou a ser o ficheiro canónico e o comentário deixou de ser verdade.
2. **O `LEGAL.md` da raiz não substitui nem toca nos textos legais.** Diz logo no topo que as páginas de privacidade, cookies e termos continuam em `apps/web-lab/` e que a redação delas não muda. É um registo de requisitos, não um parecer.
3. **No `docs/CTO.md`, a nota de cabeçalho e a secção final são minhas; o resto é transcrição.** A secção final diz que a numeração das fases das diretrizes não é a do `ROADMAP.md`, e que em caso de diferença mandam o roadmap e os ADR, que são mais recentes.
4. **Não inventei decisões de negócio.** No `NEGOCIO.md` escrevi que a data a partir da qual contam as ~6 semanas **não está decidida**, em vez de a pôr no dia do treino 01.

## 4. Grep de privacidade

Procurei em todos os ficheiros versionados e em todos os novos (fora o `package-lock.json`):

| Procura | Resultado |
|---|---|
| Utilizador do sistema e caminhos `/home/…`, `C:\Users\…`, `/mnt/c/Users/…` | **Um ficheiro, antigo** (abaixo) |
| Email pessoal (domínios comuns e o email da conta) | Nada |
| Telefone português | Nada |
| NIF | Nada |
| Morada e código postal | Só a morada pública da CNPD em `legal.privacidade.tsx` — institucional, não é do fundador |
| Profissão e empregador | Só "autorização do empregador do fundador", nos termos que o brief autoriza, e a regra do `AGENTS.md` |

**Encontrado em sessões anteriores, e não apaguei — fica à tua decisão:** o ficheiro `patches/expo-task-manager+57.0.17.patch` tem **286 linhas com caminhos absolutos de disco que incluem o utilizador do sistema**. Entraram no commit `e24db44` ("Issue a montante e prova do remendo nos dois sentidos", sessão 08). São artefactos de build do Gradle (`.../build/intermediates/...`) que o remendo apanhou por engano, e não fazem parte da correção. Duas notas:

- Regenerar o remendo sem esses ficheiros tira os caminhos de `main`, mas **não do histórico**, que já está publicado e que o `AGENTS.md` proíbe reescrever.
- Um remendo de 10 386 linhas, quase todas intermediários de build, também é frágil a atualizar, independentemente da privacidade.

**Limite do grep, dito com honestidade:** não se procura por grep um nome, uma localidade ou um empregador que não se conhecem. Os padrões acima apanham formatos; os ficheiros novos desta sessão reli-os um a um.

## 5. Teste final — o repositório lido por um agente que nunca o viu

Reli o repositório a partir do `README.md`, sem nada além do que lá está. O que ficaria por perceber, por ordem de gravidade:

1. **O `AGENTS.md` não manda ler os ficheiros novos.** A secção "Ler primeiro" continua a mandar para `STATUS` → `ROADMAP` → `VISAO` → `README` → `ARCHITECTURE`. Um agente que chegue pelo `AGENTS.md` (é o que os outros agentes leem primeiro) nunca chega ao `docs/CTO.md`, ao `NEGOCIO.md`, ao `LEGAL.md` nem ao `AMBIENTE.md`. Não mexi, porque o brief mandou manter o texto como estava (dúvida 1).
2. **O `AGENTS.md` ainda só funciona bem para o Claude.** O CTO está descrito como "o Claude do chat", e a linha de fecho dos commits é `Co-Authored-By: Claude <modelo>`. Um agente de outra casa não sabe o que escrever (dúvida 2).
3. **Duas numerações de fases.** No `docs/CTO.md` e no `NEGOCIO.md`, a "Fase 1 (MVP)" e o critério de continuar ou parar ao fim de ~6 semanas referem-se ao produto mínimo. No `ROADMAP.md` a Fase 1 foi o esqueleto Android, concluído a 2026-09-10, e **o critério das 6 semanas não aparece em lado nenhum do roadmap**. Deixei uma nota no `CTO.md`, mas quem ler o `NEGOCIO.md` sozinho fica sem saber em que fase se aplica.
4. **O ADR 0011 contradiz o que o fundador pediu no contexto de treino.** O §2 do ADR diz que os valores "entram depois de o bloco fechar". O fundador quer as duas portas, **durante o treino ou no fim**, porque os números da passadeira e do remo desaparecem se não forem registados no momento. O ADR continua proposto e não lhe toquei (dúvida 3).
5. **O "próximo passo" está enterrado.** O topo do `STATUS.md` é um parágrafo de ~40 linhas, e o "Próximo passo" outro do mesmo tamanho. A resposta a "o que faço a seguir?" — o fundador decide o ADR 0011 na quarta, e nada de código antes disso — só se encontra lendo tudo.
6. **O `README.md` tem números antigos.** Diz "169 testes" no motor e "esquema v2" na app; o `STATUS.md` diz 181 testes no motor e esquema v3, e o `npm test` da sessão 21 correu 280. Também não diz nada da Fase 4.
7. **Branches mortas no remoto.** `feat/design-na-app`, `feat/prototipo-blocos`, `feat/dogfooding-01` e `feat/marca-blender` estão todas fundidas em `main` e continuam publicadas, e nenhum ficheiro diz que estão encerradas.
8. **Os briefs não estão no repositório.** Cada relatório resume o seu, mas o texto original do CTO só existe na conversa. Um agente novo vê o que se fez, não o que se pediu; nas sessões em que o relatório se afastou do brief, isso não se reconstrói.
9. **O `AGENTS.md` não prevê sessões só de documentação.** A definition of done exige os quatro passos em "uma tarefa"; hoje e nas sessões 20 e 21 saltaram-se por instrução do brief, sem regra escrita.
10. **Referências antigas ao `CLAUDE.md`** ainda aparecem no `STATUS.md` ("regra no `CLAUDE.md`") e nos ADR 0010 e 0011. Continuam a funcionar através do ponteiro, mas mandam ler o ficheiro errado.
11. **Três leituras de "dados de saúde" que um agente novo tem de conciliar sozinho**: o `ROADMAP.md` põe a frequência cardíaca e o VO₂ fora do âmbito; o `docs/CTO.md` inclui VO₂max, FTP e HRV na visão completa; o `LEGAL.md` trata já os dados de treino como dados de saúde. Não se contradizem, porque são fases diferentes, mas nenhum ficheiro faz a ponte.

## 6. Dúvidas para o CTO

1. **Acrescento ao "Ler primeiro" do `AGENTS.md` uma linha para o `docs/CTO.md`, o `NEGOCIO.md`, o `LEGAL.md` e o `AMBIENTE.md`?** É a correção mais barata do teste final, e ficou de fora só por o brief mandar manter o texto.
2. **O `AGENTS.md` deixa de nomear o Claude?** O papel do CTO passaria a "o CTO (no chat)" e a linha dos commits a "o agente e o modelo que fizeram o trabalho", ou outra forma que decidas.
3. **O ADR 0011 precisa de acolher o registo durante o treino** antes da decisão de quarta? Hoje só prevê valores no fim.
4. **O remendo com o utilizador do sistema** (§4): regenero-o sem os intermediários de build numa sessão própria? O histórico fica como está, porque não se reescreve.
5. **Apago as quatro branches fundidas no remoto?** É destrutivo (se bem que reversível enquanto houver cópias locais), por isso não o fiz.

## 7. Próximos passos

- O fundador decide o ADR 0011 na quarta (2026-09-17). Nada de código antes disso.
- `feat/agents-md` publicada e **não fundida**: a fusão espera por ti.

---

## Sessão 22b — Fechar o repositório

**Data**: 2026-09-14 · **Branch**: continua `feat/agents-md` (de `8fc8d58`) · **Não fundida em `main`** · O CTO respondeu sim às cinco dúvidas da §6.

### Feito

| Brief | Estado |
|---|---|
| 1. O `AGENTS.md` leva aos ficheiros novos | Feito — "Ler primeiro" em três níveis (abaixo) |
| 2. O `AGENTS.md` sem nome de ferramenta | Feito — grep com limites de palavra: **zero** ocorrências |
| 3. ADR 0011 com as duas portas | Feito — secção 2a, com a razão; **continua proposto** |
| 4. Remendo regenerado | Feito — 10 386 → 36 linhas; `npm ci` aplica-o, `npm test` verde |
| 5. Numerações de fases reconciliadas | Feito — o ROADMAP é canónico; notas no `docs/CTO.md`; critério das ~6 semanas na Fase 4 do ROADMAP |
| 6. Branches fundidas apagadas no remoto | Feito — quatro, confirmadas uma a uma |

`git diff --stat main -- apps/ packages/`: **vazio**. Mudaram `patches/` (esperado) e só documentação.

### 1. "Ler primeiro" em três níveis

- **A, em todas as sessões**: `STATUS.md`, `ROADMAP.md`, `docs/VISAO.md`, o último relatório e o `git log`.
- **B, na primeira sessão de um agente neste repositório** ("na dúvida, é a primeira"), e depois sempre que o tema o pedir: `docs/CTO.md`, `LEGAL.md`, `docs/NEGOCIO.md`, `docs/AMBIENTE.md` e o **ADR 0006**. Cada um diz em que sessões é obrigatório (por exemplo, o `LEGAL.md` em tudo o que toque em dados pessoais, localização, saúde, partilha, exportação, contas, texto visível, lojas ou receita).
- **C, consulta**: `README.md`, `ARCHITECTURE.md`, a API do motor, os outros ADR e o `BACKLOG.md`, com os casos em que passam a obrigatórios.
- **A lista é o índice**: quem carregou o `AGENTS.md` fica a saber que os treze ficheiros existem e para que servem, mesmo que não os leia todos. E o "Estado lido" do relatório passa a dizer que ficheiros dos níveis B e C se leram, e porquê — assim nota-se quando um agente os saltou.
- **O ADR 0006 entrou no nível B por causa do teste final.** Na primeira versão da lista, a pergunta "porque é que é append-only" dava **não**: o STATUS e o ROADMAP dizem *que* é append-only, e só o ADR 0006 diz *porquê*.
- **No `docs/CTO.md`, a lista de leitura passou a ponteiro para o `AGENTS.md`.** A que lá estava repetia a regra com outra ordem e sem o ADR 0006, ou seja, já divergia.

### 2. Sem nome de ferramenta

O papel do CTO passou a "o agente de CTO, no chat". A linha de fecho dos commits deixou de ter fornecedor: `Co-Authored-By:` com o nome do modelo que fez o trabalho e o endereço que o fornecedor indica para essa linha; se a ferramenta propuser uma linha própria, usa-se essa, desde que nomeie o modelo real. O primeiro grep apanhou um falso positivo ("pr**opus**er"); com limites de palavra, zero. O `CLAUDE.md` continua a nomear o Claude: é o ponteiro dessa ferramenta.

### 3. ADR 0011 — as duas portas

Nova secção 2a: **durante o treino, bloco a bloco, E no fim, para completar ou corrigir**. A razão ficou escrita, para ninguém voltar a simplificar: os números da passadeira e do remo estão no mostrador da máquina e **desaparecem quando o atleta sai dela**. As duas portas escrevem da mesma maneira, como eventos acrescentados; uma correção no fim é um evento novo e nunca reescreve o anterior (ADR 0006). Entraram duas alternativas rejeitadas (só a porta do fim; corrigir reescrevendo) e um ponto novo em "o que falta decidir na quarta". **Uma parte é minha e está marcada como tal no ADR:** a regra de qual valor vale quando um bloco tem mais do que um, com a proposta "o último registado, sem apagar os anteriores". **Continua proposto.**

### 4. O remendo

- **O que tinha**: 669 ficheiros, dos quais **668 eram artefactos de build do Gradle** (`android/build/`, com caminhos de disco do utilizador do sistema). A correção a sério é **um ficheiro**, `TaskService.java`.
- **O que ficou**: esse bloco, tirado do remendo antigo tal como estava — **36 linhas**.
- **Prova de que é o mesmo remendo**: depois do `npm ci`, regenerei-o com `npx patch-package expo-task-manager` e saiu **idêntico, byte a byte**, ao que tinha extraído.
- **Prova de que se aplica**: `npm ci` (622 pacotes) → `patch-package` no `postinstall`: `expo-task-manager@57.0.17 ✔`. A linha da correção está no `TaskService.java` instalado. `npm test`: **280 testes verdes**.
- **O que não corri**: um build nativo do Android. O Java da correção é o mesmo byte a byte, por isso um APK sai igual; mas não está provado com um build.
- **Para não se repetir**: a linha do remendo no BACKLOG diz como regenerá-lo sem `android/build/`.
- **Alcance, com honestidade**: isto **limpa o `main`, não o histórico**. O commit `e24db44` continua publicado com as 286 linhas com o utilizador do sistema, e qualquer clone o tem. Tornar o repositório privado reduz muito o risco, mas não o elimina. **Uma limpeza a sério exige reescrever o histórico** (por exemplo com `git filter-repo`) e forçar o push — o que o `AGENTS.md` proíbe sem decisão explícita, e que invalida os clones existentes. **Está por fazer.**

### 5. As duas numerações de fases

- **O ROADMAP é a numeração canónica, e diz isso logo na secção "Fases".** A mesma linha estava desatualizada ("as Fases 0, 1 e 2 estão concluídas") e passou a "Fases 0 a 3 concluídas; Fase 4 em curso".
- **No `docs/CTO.md` não se apagou uma palavra das diretrizes.** Entraram três notas marcadas "sessão 22b", fora da transcrição: a "Fase 1 (MVP)" é histórica e corresponde às **Fases 1, 2 e 3** do ROADMAP; e o critério de continuar ou parar está escrito no ROADMAP, na Fase 4.
- **O critério das ~6 semanas está no ROADMAP, na Fase 4**, ao lado do critério de saída e separado dele (não é critério de saída: decide se o projeto continua). **Escolhi a Fase 4** porque é aí que corre o dogfooding que o mede — o primeiro treino foi a 2026-09-14. As diretrizes ligam-no à "Fase 1 (MVP)", que são as Fases 1 a 3, já fechadas; se preferires outro sítio, é uma linha a mudar. A data de início das 6 semanas continua por decidir.
- O `docs/NEGOCIO.md` tirou o "(Fase 1)" do título e aponta para o ROADMAP.

### 6. Branches

Confirmei uma a uma, com `git branch -r --merged main` e `git merge-base --is-ancestor`: `feat/design-na-app`, `feat/prototipo-blocos`, `feat/dogfooding-01` e `feat/marca-blender` estavam **todas fundidas em `main`**, e foram **apagadas no remoto**. A única não fundida é `feat/agents-md`, a desta sessão, que ficou. As quatro continuam como branches locais nesta máquina, o que permite recuperá-las.

**Achado a seguir:** o STATUS, o ROADMAP, o BACKLOG e o exemplo da regra 5c do `AGENTS.md` ainda diziam que `feat/marca-blender` e `feat/dogfooding-01` estavam "não fundidas" — desde a sessão 21 era falso, e com as branches apagadas mandavam um agente novo procurar uma branch que já não existe. Corrigido nos quatro sítios; a regra 5c em si não mudou.

### Privacidade

Grep ao repositório inteiro (ficheiros versionados e novos, fora o `package-lock.json`) por utilizador do sistema, email, telefone: **nada**. Com o remendo regenerado, a única ocorrência da sessão 22 desapareceu do `main`. O histórico mantém-na (§4).

### Teste final — um agente que só carregou o `AGENTS.md`

Um agente que nunca viu o repositório está, por definição, na primeira sessão: lê o nível A e o nível B. Segui esse caminho e procurei cada resposta no ficheiro onde o caminho o leva:

| Pergunta | Resposta | Onde a encontra |
|---|---|---|
| Em que fase está o projeto? | **Sim** | `STATUS.md` (A1), no topo: "Fase 4 — design e histórico … em curso"; `ROADMAP.md` (A2), "Fases": "As Fases 0 a 3 estão concluídas; a Fase 4 está em curso" |
| Porque é que o motor é append-only? | **Sim** — **na primeira versão da lista dava não**; corrigido pondo o ADR 0006 no nível B | ADR 0006 (B9): um só registo de verdade ("guardar segmentos … criaria duas verdades"), e uma escrita interrompida não corrompe uma linha confirmada. O mesmo ADR regista a exceção do `DELETE` real para apagar uma sessão a pedido do atleta |
| Os dados de treino são dados de saúde sob o artigo 9.º? | **Sim** | `LEGAL.md` (B6): "RGPD, artigo 9.º" — "Os dados de treino e o batimento cardíaco tratam-se como dados de saúde", com consentimento explícito e separado. O próprio índice do `AGENTS.md` já diz "dados de saúde (RGPD, art. 9.º)" |
| Há um critério de parar às 6 semanas? | **Sim** — **antes da 22b dava não**: só estava no `CTO.md` e no `NEGOCIO.md` | `ROADMAP.md` (A2), Fase 4, "Critério de continuar ou parar"; o `AGENTS.md` anuncia-o na descrição do ROADMAP |
| Em que máquina se faz o build? | **Sim** | `docs/AMBIENTE.md` (B8): PC com Windows 10, WSL 2 com Ubuntu, build local na WSL com JDK 17 e Android SDK, sem EAS; e já no `STATUS.md` (A1), "Ambiente de desenvolvimento" |

**Nuances do teste, ditas para não passar por mais limpo do que é:**

- **O `STATUS.md` responde à primeira pergunta, mas mal.** "Em curso" está enterrado num parágrafo de ~40 linhas. É o ROADMAP que responde numa linha.
- **"Append-only" é da persistência, não do motor.** O motor é puro e trata os eventos como verdade; append-only é a base SQLite do ADR 0006. O agente chega às duas coisas, mas a pergunta, tal como está formulada, mistura-as.
- **O nível B depende de o agente se reconhecer como novo.** Um agente com memória de outra conversa pode achar que não é a primeira sessão e saltá-lo. "Na dúvida, é a primeira" e o "Estado lido" a dizer o que se leu reduzem o risco, mas não o eliminam.

### O que ficou do teste final da sessão 22 (§5)

Resolvidos nesta sessão: 1 (lista de leitura), 2 (nome de ferramenta), 3 (numerações), 4 (duas portas) e 7 (branches). **Continuam por resolver**, porque o brief não os pedia:

- 5, o "próximo passo" enterrado no STATUS.
- 6, os números antigos no README.
- 8, os briefs fora do repositório.
- 9, a definition of done para sessões só de documentação.
- 10, as referências ao `CLAUDE.md` no STATUS e nos ADR.
- 11, a ponte entre as três leituras de "dados de saúde".

### Dúvidas para o CTO

1. **O critério das ~6 semanas na Fase 4** está no sítio certo, ou queres uma secção própria no ROADMAP, fora das fases?
2. **A limpeza do histórico** (`e24db44`): fica registada e por fazer, ou há decisão do fundador para reescrever o histórico e forçar o push?
3. **A regra "o último valor registado vale"** no ADR 0011 (secção 2a): acrescentei-a para o modelo ficar completo, e está marcada como minha. Leva-a à quarta, ou tira-a?

### Próximos passos

- O fundador decide o ADR 0011 na quarta (2026-09-17), com as duas portas.
- `feat/agents-md` publicada e **não fundida**: a fusão espera por ti.

---

## Sessão 22d — O raciocínio descartado

**Data**: 2026-09-14 · **Branch**: continua `feat/agents-md` (de `acdc653`) · **Não fundida em `main`** · Só documentação

### Feito

- **[`docs/DECISOES-DESCARTADAS.md`](../DECISOES-DESCARTADAS.md), com as sete entradas.** Todas seguem a mesma estrutura: o que se tentou, porque parecia boa ideia, porque se abandonou, a prova, e o que não se deve tentar outra vez. Cada uma tem a sessão e liga ao relatório ou ao ADR da prova. O topo explica a diferença para um ADR: os ADR registam o que se decidiu; este ficheiro regista o que se abandonou e porquê.
- **Os argumentos são os do brief, transcritos. Os factos e os números foram tirados dos relatórios e do código:**
  1. **Mudar em ciclo.** A ordem `ORDEM_MUDAR` do código da sessão 14 (`ebb09b2`) põe Força na posição 0 e Bicicleta na 6. São 6 toques e 6 `changeSport`, e ficam 5 segmentos intermédios. O relatório da 17 regista "até 7 toques" e "2 linhas, não sete" na base. O protótipo da sessão 10 conta 2 toques.
  2. **Espaço negativo.** 0 de 16 (19b §4.1), com a leitura de cada conceito (§4.2). O peso no desfoque (0,30 a 0,48, contra 0,24 da P2122) também é do relatório.
  3. **Famílias.** Os números são da tabela das 56 (18 §4 e §5) e da tabela da 19 (§4).
  4. **Ligadura BL.** O B saiu do símbolo na 19 (§10). **A aritmética da ligadura (~2 px → ~5,5 unidades) não está em nenhum relatório nem no gerador**: a entrada transcreve o argumento e diz que os números são a estimativa do CTO, não uma medida.
  5. **Duas tonalidades.** A tabela de contraste é da 19b §7: 2,22:1; 3,01:1 com o `#E0704B`; 1,64:1 entre os dois tons. Que o tom escuro desaparece sobre o campo do ícone não é uma medida: o campo é o próprio `#C0402C`.
  6. **Blender.** 200 variantes = 56 + 96 + 48. O que deu e o que não deu vêm da 18 §6, incluindo a conclusão de que um gerador de SVG com um rasterizador fazia o mesmo.
  7. **Dogfooding.** Vem do registo do treino 01, do ADR 0011 e da precedência da 4.5 no ROADMAP.
- **`AGENTS.md`**: uma linha no nível B, obrigatória em qualquer sessão que proponha uma abordagem nova. Os números dos ficheiros do nível C subiram um; o resto da secção ficou como estava.
- **`README.md`**: a entrada 4 em "Por onde começar", e o ficheiro no mapa do repositório.
- **`docs/CTO.md`**: a linha "um CTO novo lê o nível B" enumerava os ficheiros, e a lista ficou incompleta mal o nível B cresceu. Passou a apontar para o nível B do `AGENTS.md`, sem enumerar.

### Verificações

- `git diff --stat main -- apps/ packages/ tools/`: **vazio**.
- **Grep de privacidade** em todos os ficheiros versionados e novos: utilizador do sistema, email, telefone e NIF — **nada**; no ficheiro novo, também empregador, profissão, morada e localidade — **nada**.
- **Correções feitas antes do commit**, ao conferir as citações: duas secções do relatório da 18 estavam trocadas (§5 → §4 e §5); o B saiu na 19 §10, não em §1 e §3; e a frase "os segmentos não se apagam" passou a dizer o que o ADR 0006 diz — só se apaga uma sessão inteira, a pedido do atleta.

### Teste — um agente que lê só este ficheiro

| Pergunta | Resposta | Onde está |
|---|---|---|
| Porque é que o espaço negativo não se tenta outra vez? | **Sim** | Entrada 2: a razão é estrutural — a dupla leitura precisa de uma forma que contenha, e essa forma é a moldura, eliminada na 18 (entrada 3); sem recipiente, o olho lê a peça de fora antes do vazio; com cortes a 90° e 24 px, o truque fica indisponível dentro do DESIGN.md. Prova: 0 de 16. Regra: não voltar sem mudar primeiro as regras do DESIGN.md, que é decisão do fundador |
| Porque é que o Mudar não pode ciclar? | **Sim** | Entrada 1: cada toque escrevia um `changeSport`, e de Força a Bicicleta ficavam 5 segmentos de ~0 s numa base append-only — dano nos dados, não um problema de interface; e contrariava os 2 toques do fluxo aprovado. Regra: nenhum atalho de interface escreve eventos que o atleta não pediu; propor não é escrever |

As duas respostas estão no próprio ficheiro, sem precisar de abrir os relatórios. A entrada 2 depende da entrada 3 para "a moldura foi eliminada", mas as duas estão no mesmo ficheiro.

### Dúvida para o CTO

- **A entrada 4 é a única sem prova no repositório.** Os números da ligadura são teus e nunca foram medidos. Queres que a próxima sessão da marca, quando sair do standby, os meça antes de qualquer desenho?

### Próximos passos

- A fusão de `feat/agents-md` em `main` é a sessão seguinte.
- O fundador decide o ADR 0011 na quarta (2026-09-17).

---

## Sessão 22c — Fechar e fundir

**Data**: 2026-09-14 · **Branch**: `feat/agents-md` (de `1cbcc5c`), **fundida em `main`** · Só documentação

### Feito

- **Respostas do CTO aplicadas:**
  - **Entrada 4 de `DECISOES-DESCARTADAS.md`:** a ligadura BL fica como estimativa **não medida**, e medi-la na cadeia do Blender (24, 48 e 73 px, com o recorte circular) é o primeiro passo quando a marca sair do standby.
  - **BACKLOG:** limpar o histórico do remendo passa a ser pré-requisito de qualquer reabertura do repositório ao público; **a limpeza não se fez**.
  - **ADR 0011:** "vale o último valor registado" mantém-se como proposta, e o ADR **continua proposto**.
  - **Critério das 6 semanas:** fica na Fase 4, sem secção própria.
- **STATUS.md:** abre agora com "Em resumo" — fase atual, feito, agora, a seguir, critério de parar, standby, e por onde começar. A linha de 1 809 caracteres passou inteira para "O caminho até aqui, fase a fase", e a informação das branches da sessão 21 ficou na mesma secção. Não se apagou nada.
- **Merge:** `feat/agents-md` fundida em `main` por **fast-forward** (`main` estava em `c7113aa`, antepassado da branch), sem conflitos. `main` = `origin/main` = `9587fcf` antes deste relatório. `git diff --stat c7113aa main -- apps/ packages/ tools/`: vazio. A branch **não foi apagada**.

### DoD em `main`

| Passo | Resultado |
|---|---|
| `npm ci` | 622 pacotes; `patch-package`: `expo-task-manager@57.0.17 ✔` com o remendo regenerado |
| `npm test` | **280 testes**, 20 ficheiros, verdes |
| Cobertura do motor | **100 %**: 287/287 declarações, 186/186 ramos, 60/60 funções, 245/245 linhas. O total do `test:coverage` (98,94 %) inclui o adaptador de persistência da app (`repository.ts` 97,65 %, `writeCost.ts` 96 % em linhas), que nenhuma destas sessões tocou |
| `npm run typecheck` | Verde nos quatro workspaces, zero erros |
| `npm run build:web` | Verde |
| `npm run export:android -w @bricklap/mobile` | Verde (801 módulos) |

### Teste final, a partir de `main`

Clone novo de `origin/main` (`9587fcf`) numa pasta à parte, a seguir só o "Ler primeiro" do `AGENTS.md`. Os treze ficheiros que ele manda ler existem no clone, e o `AGENTS.md` lista todos. Grep de privacidade no clone: nada.

| Pergunta | Resposta | Onde o agente a encontra |
|---|---|---|
| Em que fase está o projeto? | **Sim** | `STATUS.md` (A1), primeira linha do resumo: "Fase atual: Fase 4 — design e histórico, em curso"; `ROADMAP.md` (A2): "As Fases 0 a 3 estão concluídas; a Fase 4 está em curso" |
| Porque é que a persistência é append-only? | **Sim** | ADR 0006 (B9): um só registo de verdade ("guardar segmentos … criaria duas verdades") e "uma escrita interrompida a meio nunca corrompe uma linha confirmada" |
| Os dados de treino são dados de saúde do artigo 9.º? | **Sim** | `LEGAL.md` (B6): "RGPD, artigo 9.º" — "Os dados de treino e o batimento cardíaco tratam-se como dados de saúde" |
| Há um critério de parar às 6 semanas? | **Sim** | `ROADMAP.md` (A2), Fase 4, "Critério de continuar ou parar"; e já no resumo do `STATUS.md` (A1) |
| Em que máquina se faz o build? | **Sim** | `docs/AMBIENTE.md` (B8): PC com Windows 10, WSL 2 com Ubuntu, build local na WSL |
| Porque é que não se volta a tentar espaço negativo? | **Sim** | `docs/DECISOES-DESCARTADAS.md` (B10), entrada 2: razão estrutural — sem a moldura não há recipiente, e o olho lê a peça de fora antes do vazio; 0 de 16; não voltar sem mudar primeiro o DESIGN.md |

**Seis sim, a partir de `main`.**
